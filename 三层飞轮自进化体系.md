# 三层飞轮自进化体系

> **核心思想**：让股票反转检测系统在无人干预的情况下，每天自己记录、自己复盘、自己变强。飞轮不追求"跑通一次"，追求"每跑一次就增重一点"——最终形成自我强化的势能。

---

## 一、飞轮的本质：不是"循环"，是"势能积累"

### 1.1 普通流水线 vs 飞轮

绝大多数量化系统是 **pipeline（流水线）**：

```
流水线:  1→2→3→4→1→2→3→4    每次从 0 开始跑，跑完清零
```

而飞轮的核心区别是 **每一圈都在积累势能**：

```
飞轮:    1→2→3→4            跑一圈，飞轮变重了
         1→2→3→4            第二圈，惯性帮你省力气
         1→2→3→4            第三圈，越来越快，越来越稳
         ...                最终自动旋转
```

Jim Collins 在《从优秀到卓越》里的原话：

> *"A flywheel is heavy. You push hard, it barely moves. Then it picks up speed. And suddenly it's spinning on its own."*

### 1.2 为什么你的系统需要飞轮？

你的反转检测系统（`reversal_detector.py`）已经实现了 8 类信号、4 种市场状态、多因子加权打分。但目前有一个问题：

```
信号权重（trending_up 里 divergence=1.3, structure=1.2...）
    ↑ 这些数字是怎么来的？
    ↑ 你凭什么觉得 divergence 比 structure 重要 10%？
    ↑ 有没有验证过？
```

传统做法是：手动调参 → 回测 → 手动改 → 再回测。这个过程费时、主观、而且做完就断了——你不会每次有新数据都重新跑一遍调参。

飞轮把这个过程变成 **自动化、持续化、结构化** 的闭环：

```
每天自动记录信号 → 自动追踪效果 → 自动统计胜率 → 自动调权重 → 明天用新权重扫描 → 继续记录 → ...
```

### 1.3 三层飞轮的整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  🧠 认知飞轮（Cognitive Flywheel）   ← 发现"新东西"              │
│     "还有什么我们没发现的信号？有没有更好的因子组合？"              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  📊 数据飞轮（Data Flywheel）    ← 让"老东西更准"           │  │
│  │     "背离信号在震荡市到底行不行？权重应该是多少？"              │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐   │  │
│  │  │  🛠️ 工程飞轮（Engineering Flywheel） ← 让"系统更稳"    │   │  │
│  │  │     "API 超时？缓存 TTL 自动调；哪个股票扫描最慢？"      │   │  │
│  │  └────────────────────────────────────────────────────┘   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                  │
│  触发频率: 工程飞轮(每次) → 数据飞轮(每日) → 认知飞轮(每周)      │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

三层飞轮的依赖关系（自底向上）：

```
认知飞轮（发现什么好）
    │ 需要高质量的统计数据
    ▼
数据飞轮（验证好的有多少 → 量化权重）
    │ 需要系统跑得动、跑得稳
    ▼
工程飞轮（让前两个跑得动）
```

**工程飞轮不转 → 数据攒不起来 → 数据飞轮没料 → 认知飞轮没基础。**

---

## 二、数据飞轮 —— 让信号自己变强

### 2.1 解决什么问题？

你的系统有 8 类反转信号，每类在不同市场状态下有不同权重。但这些权重是**专家经验**定的，不是**数据证明**的。数据飞轮要把专家经验转化为统计规律。

```
现在:   你觉得 "divergence 在趋势里应该更重要" → 手动设 1.3
之后:   系统自己算出 "divergence 在 trending_up 里胜率 63.5%，
        structure 胜率 48.2%" → divergence 自动升到 1.45，
        structure 降到 0.92
```

### 2.2 运行机制

```
┌────────────────────────────────────────────────────────────┐
│                                                           │
│   [Step 1] 扫描 & 记录（每日收盘后）                        │
│   ┌──────────────────────────────────────────────────┐    │
│   │ 603259 触发 bearish reversal                      │    │
│   │ confidence=85%, regime=trending_up                │    │
│   │ signals=[structure, crowding, extremum]           │    │
│   │ ↓ 写入 data/signal_log.jsonl                      │    │
│   └──────────────────────────────────────────────────┘    │
│                                                           │
│   [Step 2] 追踪 & 回查（每日）                              │
│   ┌──────────────────────────────────────────────────┐    │
│   │ 5 天前 002415 的 bearish 信号                      │    │
│   │ 后来跌了 3.2% → ✅ correct=true                    │    │
│   │ ↓ 把正确/错误 + 实际收益标回那条记录                 │    │
│   └──────────────────────────────────────────────────┘    │
│                                                           │
│   [Step 3] 交叉统计（每日）                                 │
│   ┌──────────────────────────────────────────────────┐    │
│   │ 按 (regime × signal_type) 分组:                   │    │
│   │                                                   │    │
│   │ trending_up + structure   → n=24, wr=66.7%, +0.8%│    │
│   │ trending_up + divergence  → n=18, wr=63.5%, +0.6%│    │
│   │ trending_up + crowding    → n=12, wr=41.7%, -0.3%│    │
│   │ ranging     + extremum    → n=30, wr=70.0%, +1.1%│    │
│   │ trending_down + crossover → n=15, wr=53.3%, +0.2%│    │
│   └──────────────────────────────────────────────────┘    │
│                                                           │
│   [Step 4] 自动调权（每日/每周）                            │
│   ┌──────────────────────────────────────────────────┐    │
│   │ 胜率 > 60% → 权重 ×1.15（上限 2.0）                │    │
│   │ 胜率 < 40% → 权重 ×0.85（下限 0.5）                │    │
│   │ 中间区间 → 不动                                    │    │
│   │                                                   │    │
│   │ structure: 1.20 → 1.38 ↑   (wr=66.7%)            │    │
│   │ crowding:  1.00 → 0.85 ↓   (wr=41.7%)            │    │
│   │ extremum:  1.20 → 1.38 ↑   (wr=70.0%, ranging)   │    │
│   │ ↓ 写回 config/flywheel_weights.json                │    │
│   └──────────────────────────────────────────────────┘    │
│                                                           │
│   ⚡ 下次扫描时，新权重自动生效                              │
│                                                           │
└────────────────────────────────────────────────────────────┘
```

### 2.3 核心洞察：结构化记录是飞轮的燃料

你现在复盘会写：

> "这个交易亏了，因为我追高了"

但这句话 **不能被程序统计**。数据飞轮要求你把每次决策的 context 结构化：

```json
{
  "scan_date": "2026-08-10",
  "stock_code": "603259",
  "current_price": 154.82,
  "market_regime": "trending_up",
  "net_signal": -0.300,
  "direction": "bearish",
  "confidence": 0.85,
  "total_signals": 5,
  "bull_count": 2,
  "bear_count": 3,
  "signal_types": ["structure", "crowding", "extremum"],
  "tracked": true,
  "correct": true,
  "trade_return_pct": -3.2,
  "entry_price_tracked": 154.82,
  "exit_price": 150.05,
  "tracked_date": "2026-08-10"
}
```

有了这种结构化记录，你就能问出**无数个之前想都没想过的问题**：

| 你能问的问题 | 数据飞轮的回答 |
|-------------|--------------|
| RSI > 75 时的信号胜率比 RSI < 60 低多少？ | 统计 extremum 信号在不同 RSI 区间的胜率 |
| 偏离 MA5 超过 8% 的信号是不是 80% 都会回调？ | 计算 `(price - MA5) / MA5` 与 outcome 的相关性 |
| 有 crowding 信号参与的组合平均收益是不是低 2%？ | 对比含 crowding vs 不含 crowding 的 outcome |
| 3+ 信号共振是不是比单信号胜率高？ | 分组统计 `total_signals >= 3` vs `=1` |
| trending_up 和 ranging 里哪些信号效果反转？ | 交叉表：regime × signal_type × win_rate |

### 2.4 数据文件与代码对应

| 文件/模块 | 作用 | 写入时机 |
|----------|------|---------|
| `data/signal_log.jsonl` | 所有历史信号记录（含追踪结果） | 每日扫描 + 每日追踪 |
| `config/flywheel_weights.json` | 当前生效的权重配置 | 每周调优 |
| `logs/weight_adjustments.jsonl` | 每次权重调整的历史（可审计） | 调优时 |
| `DataFlywheel.scan_and_log()` | 扫描股票池，记录今日信号 | 每日 |
| `DataFlywheel.track_outcomes()` | 回查历史信号效果 | 每日 |
| `DataFlywheel.compute_signal_stats()` | 交叉统计胜率分布 | 每日 |
| `DataFlywheel.optimize_weights()` | 根据统计自动调优权重 | 每周 |

### 2.5 使用方法

```bash
# 每日完整循环（扫描+追踪+统计+调优）
python3 src/flywheel_orchestrator.py --cycle daily

# 只调权重（不等一个完整周期）
python3 src/flywheel_orchestrator.py --optimize-weights

# 限制股票池
python3 src/flywheel_orchestrator.py --cycle daily --pool 002415 600519
```

---

## 三、认知飞轮 —— 让系统自己发现新规律

### 3.1 解决什么问题？

数据飞轮调的是**你已经定义好的信号的权重**。但如果你定义的 8 类信号里，**没有第 9 类特别好用的信号**呢？

```
数据飞轮:   "structure 在 trending_up 里权重是 1.2 还是 1.38？"
            → 调的是"你已经在追踪的东西的参数"

认知飞轮:   "如果 structure + divergence 同时出现，胜率是不是比
            单独出现高 15%？这个组合我之前想都没想过！"
            → 发现的是"你之前没考虑过的东西"
```

### 3.2 运行机制

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│   [Step 1] 提出候选假设                                  │
│   ┌──────────────────────────────────────────────────┐ │
│   │ 来源 1: 人类研究员                                │ │
│   │   "我觉得 RSI 拐头 + 放量 可能比 RSI 单独拐头准"    │ │
│   │                                                   │ │
│   │ 来源 2: AI 自动生成                               │ │
│   │   LLM 读历史数据 → 生成 3-5 个候选假设              │ │
│   │                                                   │ │
│   │ 假设列表:                                         │ │
│   │   H1: RSI<25 拐头 + 量比>1.5 → 胜率提升 >10%       │ │
│   │   H2: 3+ 信号共振 → 胜率提升 >15%                  │ │
│   │   H3: trending_up + crowding → 反转概率被低估       │ │
│   │   H4: 高低点抬高 + 缩量 → 底部确认更可靠            │ │
│   └──────────────────────────────────────────────────┘ │
│                                                         │
│   [Step 2] 批量回测验证                                  │
│   ┌──────────────────────────────────────────────────┐ │
│   │ H1 验证:                                          │ │
│   │   - RSI 单独拐头: 胜率 52% (n=38)                 │ │
│   │   - RSI 拐头+放量: 胜率 64% (n=12)                │ │
│   │   → ✅ 提升 12%，样本充足，验证成立                 │ │
│   │                                                   │ │
│   │ H2 验证:                                          │ │
│   │   - 单信号: 胜率 48%                              │ │
│   │   - 3+信号: 胜率 62%                              │ │
│   │   → ✅ 提升 14%，验证成立                          │ │
│   │                                                   │ │
│   │ H3 验证:                                          │ │
│   │   - ❌ trending_up + crowding 效果不稳定            │ │
│   │   - 样本不够 / 胜率方差大 → 暂不采纳                │ │
│   │                                                   │ │
│   │ H4 验证:                                          │ │
│   │   - 高低点抬高+缩量: 胜率 71% (n=7)               │ │
│   │   - ❌ 样本太少，需继续观察                         │ │
│   └──────────────────────────────────────────────────┘ │
│                                                         │
│   [Step 3] 把发现升级为系统规则                          │
│   ┌──────────────────────────────────────────────────┐ │
│   │ ✅ H1 → 新增复合信号规则:                          │ │
│   │   extremum 权重在 vol_ratio > 1.5 时 ×1.2          │ │
│   │   ↓ 写进 reversal_detector.py 的信号检测逻辑       │ │
│   │                                                   │ │
│   │ ✅ H2 → 置信度计算里的"信号共振加成"已经在运行了    │ │
│   │   可以考虑把加成系数从 1.1 提到 1.15               │ │
│   │                                                   │ │
│   │ ❌ H3, H4 → 不采纳 / 继续观察                      │ │
│   └──────────────────────────────────────────────────┘ │
│                                                         │
│   [Step 4] 记录结论到 hypotheses.jsonl                  │
│   下次 LLM 生成假设时会参考历史结论，不重复踩坑          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.3 关键洞察：认知飞轮 = AI 替你当研究员

你写 `reversal_detector.py` 时，手动设计了 8 类信号。但你怎么知道只有这 8 类？有没有第 9 类信号，效果比这 8 类都好，但你没想到？

认知飞轮就是让 AI 替你做这件事：

```python
# 未来升级为 LLM 驱动的假设生成（伪代码）
prompt = f"""
我们有 3000 条历史信号记录，覆盖 20 只股票，5 种市场状态。

已验证有效的信号模式: {validated_patterns}
已验证无效的假设:     {rejected_hypotheses}

请提出 3 个你认为最值得验证的新假设。
每个假设需要说明:
  - 假设内容（描述一个信号组合或条件）
  - 预期提升幅度
  - 需要多少样本才能验证

返回 JSON 数组。
"""

hypotheses = llm.generate(prompt)
for h in hypotheses:
    result = run_backtest(h)
    if result.win_rate > 0.6 and result.sample >= 15:
        implement(h)  # 注册为新信号 / 更新权重规则
```

### 3.4 复合信号发现示例

这是认知飞轮可能帮你发现的一些你之前没考虑过的规则：

| 发现 | 含义 | 如何升级 |
|------|------|---------|
| RSI 拐头 + 量比 > 1.5 → 胜率 +12% | 单独 RSI 信号容易是"假拐头"，放量确认后更可靠 | 在 `detect_rsi_extremum()` 里加量比过滤器 |
| 连续 3 天结构破坏 + 缩量 → 胜率 +18% | "衰竭性突破"才是真反转，放量突破反而可能是中继 | 新增 `detect_exhaustion_breakout()` 信号 |
| trending_down 里 divergence 权重比 trending_up 高 25% | 下跌趋势里的底背离比上涨趋势里的顶背离更准 | 分开设置上涨/下跌趋势里的 divergence 权重 |
| 换手率 < 1% 的股票，所有信号胜率都低 15% | 流动性差的股票信号无效 | 加换手率过滤器，自动跳过冷门股 |

### 3.5 在你的 10 步投资流水线里的位置

```
④ 公司研究  → 认知飞轮发现新因子
              例: "ROE 连续 3 季下滑 + PE 分位数 < 20% → 
              反转概率 68%" → 这是之前没想到的基本面因子

⑤ 因子打分  → 数据飞轮调权重 + 认知飞轮发现复合因子
              例: 数据飞轮说 structure 权重 1.38；
              认知飞轮说还要加 "structure+放量" 复合因子

⑩ 归因复盘  → 认知飞轮区分"运气收益"和"方法论收益"
              例: "这次盈利是因为选对了赛道 (运气)，
              还是因为 structure 信号起了作用 (方法论)？"
              → 方法论收益的部分 → 升级为新规则
```

### 3.6 使用方法

```bash
# 单独跑认知飞轮（生成+验证假设）
python3 src/flywheel_orchestrator.py --hypotheses

# 完整每周循环（数据飞轮调权 + 认知飞轮挖模式）
python3 src/flywheel_orchestrator.py --cycle weekly
```

---

## 四、工程飞轮 —— 让系统本身越跑越稳

### 4.1 解决什么问题？

前两个飞轮优化的是**策略质量**（什么信号好）。工程飞轮优化的是**系统质量**：

- 如果新浪 API 三天两头超时 → 信号记录不全 → 数据飞轮统计不准
- 如果每次扫描要 10 秒一只 → 50 只就要 8 分钟 → 没法高频跑
- 如果磁盘满了 → 日志写不进去 → 飞轮直接停转

**工程飞轮是前两个飞轮的地基。**

### 4.2 运行机制

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│   [每次扫描自动记录性能]                                    │
│   {                                                        │
│     "stock": "002415",                                     │
│     "total_ms": 1850,                                      │
│     "breakdown": {                                         │
│       "api_fetch_ms": 1200,     ← 数据获取耗时            │
│       "indicator_ms": 650,      ← 指标计算耗时            │
│       "signal_ms": 0,           ← 信号检测耗时（含在calc） │
│     },                                                     │
│     "signals_found": 4,                                    │
│     "timestamp": "2026-08-10 15:05:03"                     │
│   }                                                        │
│   ↓ 写入 logs/perf_log.jsonl                                │
│                                                            │
│   [每周自动做健康检查]                                      │
│   ┌────────────────────────────────────────────────────┐  │
│   │ ✅ 权重配置       → OK（config/flywheel_weights.json 可读）│  │
│   │ ✅ 数据获取 API   → 240 条数据 in 1200ms（正常）       │  │
│   │ ✅ 信号检测       → 197ms, 4 signals（正常）            │  │
│   │ ✅ 回测功能       → 750ms, 96 signals（正常）           │  │
│   │ ✅ 磁盘空间       → 767.0GB free（充足）                │  │
│   │ 📈 数据飞轮       → 35 条信号, 20 条已追踪 (57%)        │  │
│   │ 📈 认知飞轮       → 3 个假设, 3 个验证成立              │  │
│   │ ⚠️  API 可用性    → 过去 7 天有 2 次超时（需要关注）    │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
│   [自动生成优化建议]                                        │
│   ┌────────────────────────────────────────────────────┐  │
│   │ 🔴 P95 扫描耗时 5.2s > 阈值 5s                      │  │
│   │    → 建议: 给非活跃股票加 15 分钟缓存 TTL             │  │
│   │                                                      │  │
│   │ 🟡 新浪 API 偶发超时 (2/7天)                        │  │
│   │    → 建议: 把 akshare 重试次数从 2 调到 3            │  │
│   │                                                      │  │
│   │ 🟢 缓存命中率 72% → 良好                             │  │
│   │                                                      │  │
│   │ 🟢 磁盘剩余 767GB → 不用管                           │  │
│   └────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### 4.3 关键洞察：把运维经验变成自动化

你作为开发者，每次遇到这些问题：

> "API 好慢啊"、"那个股票扫描总超时"、"日志文件又忘了清理"、"缓存设多久合适？"

这些判断和行动本质上都是**对系统进行优化**。但如果每次都是你手动发现、手动修，那系统就不会自己变好。

工程飞轮把这个过程变成自动化：

```
原来:   你发现 API 慢 → 你手动加缓存 → 好一段时间 → 又慢了 → 你手动调缓存
飞轮后: 系统自动检测 P95 耗时 → 自动建议 TTL 调整 → 你一键确认 → 生效
```

### 4.4 智能缓存 TTL（已经实现）

代码里有一个基于时间段自动计算缓存有效期的函数：

```python
def suggest_cache_ttl() -> int:
    hour = datetime.now().hour
    weekday = datetime.now().weekday()
    if weekday >= 5:   return 60 * 8     # 周末：日线数据不变，缓存 8 小时
    if 9 <= hour < 15: return 15         # 盘中：15 分钟足够覆盖波动
    return 60 * 2                        # 盘后：价格定了，缓存 2 小时
```

未来可以升级为**基于历史数据的自适应 TTL**：

```python
# 未来版：根据某只股票的历史波动情况动态调整
def adaptive_ttl(stock_code: str) -> int:
    volatility = get_20d_volatility(stock_code)
    base_ttl = suggest_cache_ttl()
    if volatility > 0.03:  # 高波动 → 缓存更短
        return max(5, base_ttl // 2)
    if volatility < 0.01:  # 低波动 → 缓存更长
        return base_ttl * 2
    return base_ttl
```

### 4.5 在你的 AI Agent 投资流水线里的位置

```
③ 信息收集  → API 可用性、数据源覆盖度、缓存命中率
              对应 H2 硬约束: API 必须走容错层
              → 工程飞轮监控容错层的实际表现
              → 自动发现 "这个数据源经常超时，要不要加一个备用？"

⑦ 回测校验  → 回测速度够不够？有没有跑不完的情况？
              → 如果某只股票回测 P95 > 10s → 建议优化

⑧ 风险检查  → 工程飞轮可以监控：
              - 风控规则是否被绕过
              - 仓位数据是否异常
              - 是否有未授权的交易

⑨ 调仓执行  → 记录执行延迟、滑点、下单成功率
              → "这只股票每次都在关键价位滑点严重" → 建议改限价单策略

H3 硬约束: 所有交易可追溯
              → 工程飞轮自动记录每笔交易的完整 context
```

### 4.6 使用方法

```bash
# 立即跑健康检查
python3 src/flywheel_orchestrator.py --health

# 查看系统当前状态仪表盘
python3 src/flywheel_orchestrator.py --status
```

---

## 五、三层飞轮串联：在 AI Agent 投资流水线里的完整闭环

### 5.1 你的 10 步流水线 × 三层飞轮

回到你在 `docs/AI量化投资流水线.md` 里定义的 10 步体系：

```
     ① 投资信仰
     └─ 认知飞轮喂料: "过去 3 年哪些赛道的反转信号最有效？"
                    → 帮你修正信仰边界

     ② 赛道选择
     └─ 数据飞轮: "新能源信号胜率 52% vs 半导体 64%"
                    → 自动给赛道打"信号可靠性"分

     ③ 信息收集
     └─ 工程飞轮:  API 健康度、数据源覆盖度、缓存命中率

     ④ 公司研究
     └─ 认知飞轮: "AI 发现 ROE 下滑 + PE 低位 → 反转概率 68%"
                    → 给研究员提示新的观察维度

     ⑤ 因子打分
     └─ 数据飞轮:  权重自动调优 + 因子有效性统计
     └─ 认知飞轮:  复合因子发现（如 "结构破坏+放量"）

     ⑥ 组合构建
     └─ 数据飞轮:  哪些因子组合的信息比率最高？

     ⑦ 回测校验
     └─ 数据飞轮:  回测过的策略 → 每天自动追踪实际效果
     └─ 认知飞轮:  回测里表现好的假设 → 考虑升级为正式因子

     ⑧ 风险检查
     └─ 认知飞轮:  "有没有我没考虑过的风险模式？"

     ⑨ 调仓执行
     └─ 工程飞轮:  执行延迟、滑点记录、下单成功率

     ⑩ 归因复盘
     └─ 数据飞轮:  每笔交易的正确/错误自动统计
     └─ 认知飞轮:  区分"运气收益"和"方法论收益"
                    → 方法论收益 → 升级为新规则
     └─ 工程飞轮:  复盘报告自动生成

              ↓ 复盘结论回流 ↓
              投资信仰被更新 → 下一圈起点更高
```

### 5.2 飞轮的自动迭代节奏

```
每日 (收盘后 15:05)
├── 工程飞轮: 记录今日扫描性能
├── 数据飞轮: 扫描 → 记录信号 → 追踪历史 → 统计胜率
└── 输出: 今日信号日报 + 胜率更新

每周五 (16:00)
├── 数据飞轮: 周度权重调优（基于本周新增数据）
├── 认知飞轮: 生成假设 → 批量回测 → 发现新规则
├── 工程飞轮: 全链路健康检查 + 优化建议
└── 输出: 周度仪表盘 + 假设验证报告 + 权重变更列表

每月 (最后一个交易日)
├── 认知飞轮: 月度深度复盘
│   - 哪些策略贡献了正收益？
│   - 哪些是运气，哪些是方法论？
│   - 投资信仰要不要调整？
├── 数据飞轮: 全月胜率分布 + 权重稳定性分析
└── 工程飞轮: 月度性能报告 + 长期趋势预警
```

### 5.3 未来 AI Agent 投资系统的飞轮架构

当你把 10 步流水线逐步交给 AI Agent 执行时，三层飞轮会演化成：

```
┌─────────────────────────────────────────────────────────────┐
│                     AI Agent 投资系统                        │
│                                                             │
│  ┌──────────────────┐  ┌──────────────────┐               │
│  │  赛道研究员 Agent  │  │  公司分析师 Agent  │               │
│  │  (赛道景气度打分)  │  │  (深度研究报告)    │               │
│  └────────┬─────────┘  └────────┬─────────┘               │
│           │                      │                          │
│           └──────────┬───────────┘                          │
│                      ↓                                      │
│              ┌───────────────┐                              │
│              │  因子打分 Agent │                              │
│              │  (数据飞轮驱动) │                              │
│              └───────┬───────┘                              │
│                      │                                      │
│                      ↓                                      │
│              ┌───────────────┐                              │
│              │  组合构建 Agent │                              │
│              └───────┬───────┘                              │
│                      │                                      │
│                      ↓                                      │
│         ┌─────────────────────────────┐                    │
│         │      决策层（人 + Agent）       │                    │
│         │  ┌─────────────────────┐   │                    │
│         │  │ 认知飞轮 审核假设     │   │                    │
│         │  │ 工程飞轮 检查健康度   │   │                    │
│         │  └─────────────────────┘   │                    │
│         └──────────┬──────────────────┘                    │
│                    ↓                                        │
│            ┌───────────────┐                                │
│            │  执行 Agent    │                                │
│            │  + 追踪 Agent   │ ← 数据飞轮的信号追踪          │
│            └───────┬───────┘                                │
│                    │                                        │
│                    ↓                                        │
│         ┌──────────────────────────────┐                   │
│         │        归因复盘 Agent          │                   │
│         │  (数据飞轮统计 + 认知飞轮分析)  │                   │
│         └──────────────────────────────┘                   │
│                    │                                        │
│                    ↓ 结论回流                                │
│         ┌──────────────────────────────┐                   │
│         │     更新投资信仰 & 规则库       │                   │
│         │  → 下一圈流水线起点更高          │                   │
│         └──────────────────────────────┘                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 六、实操指南

### 6.1 快速启动（3 步跑通飞轮）

```bash
# Step 1: 健康检查（确认工程飞轮能启动）
python3 src/flywheel_orchestrator.py --health

# Step 2: 跑每日循环（开始攒数据）
python3 src/flywheel_orchestrator.py --cycle daily --pool 002415 600519 300750

# Step 3: 设置 crontab 自动化
crontab -e
# 每周一到周五 15:05 自动跑
0 15 * * 1-5 cd /Users/xkx/股票分析 && /usr/bin/python3 src/flywheel_orchestrator.py --cycle daily >> logs/cron.log 2>&1
# 每周五 16:00 跑完整链路
0 16 * * 5 cd /Users/xkx/股票分析 && /usr/bin/python3 src/flywheel_orchestrator.py --cycle weekly >> logs/cron.log 2>&1
```

### 6.2 观察飞轮是否在"增重"

跑满两周后，你应该能看到这些变化：

| 指标 | 初始状态 | 两周后应该看到 |
|------|---------|--------------|
| `signal_log.jsonl` 记录数 | 0 | > 100 |
| `config/flywheel_weights.json` 变更次数 | 0 | ≥ 1 次 |
| 认知飞轮假设数 | 0 | ≥ 3 个，至少 1 个验证成立 |
| 工程飞轮 P95 耗时趋势 | 基线 | 持平或下降（缓存生效） |

### 6.3 什么时候该升级？

```
跑满 1 个月后:
├── 信号记录 > 300 条 → 数据飞轮权重调优已经有统计显著性
├── 至少 1 个认知飞轮假设被采纳 → 系统已经不是"初始版本"了
└── 可以考虑:
    ├── 接入 LLM 自动生成假设（认知飞轮升级）
    ├── 把飞轮扩展到基本面因子（数据飞轮覆盖更广）
    └── 接入真实交易系统（工程飞轮延伸到执行层）
```

---

## 七、总结

### 三个飞轮的公式

| 飞轮 | 一句话公式 | 你的场景里的含义 |
|------|----------|----------------|
| **数据飞轮** | `信号记录 → 效果验证 → 权重自动调优` | "这个信号到底准不准，让数据说话" |
| **认知飞轮** | `假设生成 → 批量回测 → 新信号发现` | "有没有我没想到的信号组合？AI 帮我想" |
| **工程飞轮** | `性能监控 → 瓶颈定位 → 自动优化` | "系统跑得够快够稳吗？自己监控自己" |

### 飞轮的魔法不是做了什么，而是**每次做完，下次自动变得更好**

传统量化系统的路径：

```
写代码 → 手动调参 → 跑回测 → 手动改 → 再跑 → 停
```

飞轮驱动系统的路径：

```
写代码 → 跑 → 自动记录 → 自动统计 → 自动调优 → 跑 → 继续记录 → 继续调优 → ...
                                                      ↑
                                            不需要你手动改一行代码
```

这就是从**静态工具**到**自进化系统**的本质区别——也是你投资体系的"护城河"。

---

## 附录：文件位置索引

| 项目 | 路径 |
|------|------|
| 飞轮调度器代码 | `src/flywheel_orchestrator.py` |
| 核心信号检测 | `src/reversal_detector.py` |
| 信号记录日志 | `data/signal_log.jsonl`（自动生成） |
| 权重配置 | `config/flywheel_weights.json`（自动生成/更新） |
| 权重调整历史 | `logs/weight_adjustments.jsonl` |
| 性能日志 | `logs/perf_log.jsonl` |
| 假设数据库 | `data/hypotheses.jsonl` |
| 本文档 | `docs/三层飞轮自进化体系.md` |
| 10 步投资流水线 | `docs/AI量化投资流水线.md` |
